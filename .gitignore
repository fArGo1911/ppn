# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env — NEVER commit real secrets/keys; keep only the example.
.env
.env.*
!.env.example

# Supabase local state + dumps (config.toml + migrations/** + seed.sql ARE committed — the reproducible layer).
supabase/.branches/
supabase/.temp/
*.dump
*.sql.gz

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
